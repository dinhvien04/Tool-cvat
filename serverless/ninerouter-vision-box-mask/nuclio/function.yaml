metadata:
  name: ninerouter-vision-box-mask
  namespace: cvat
  annotations:
    name: 9Router Vision Box+Mask
    type: detector
    spec: |
      [
        { "id": 1, "name": "pedestrian", "type": "any" },
        { "id": 2, "name": "rider", "type": "any" },
        { "id": 3, "name": "car", "type": "any" },
        { "id": 4, "name": "truck", "type": "any" },
        { "id": 5, "name": "bus", "type": "any" },
        { "id": 6, "name": "train", "type": "any" },
        { "id": 7, "name": "motorcycle", "type": "any" },
        { "id": 8, "name": "bicycle", "type": "any" },
        { "id": 9, "name": "traffic light", "type": "any" },
        { "id": 10, "name": "traffic sign", "type": "any" },
        { "id": 11, "name": "person", "type": "any" },
        { "id": 12, "name": "traffic_light", "type": "any" },
        { "id": 13, "name": "traffic_sign", "type": "any" }
      ]

spec:
  description: 9Router Vision Box+Mask AI Detector (Bounding Box and Instance Segmentation via local 9Router)
  runtime: 'python:3.11'
  handler: main:handler
  eventTimeout: 120s

  env:
    - name: NINEROUTER_URL
      value: 'http://host.docker.internal:20128'
    - name: VISION_MODEL
      value: ''
    - name: DETECTION_MODE
      value: 'box_and_mask'
    - name: NINEROUTER_TIMEOUT
      value: '60.0'
    - name: MAX_IMAGE_SIZE
      value: '1600'

  triggers:
    myHttpTrigger:
      numWorkers: 2
      kind: 'http'
      workerAvailabilityTimeoutMilliseconds: 10000
      attributes:
        maxRequestBodySize: 33554432 # 32MB

  build:
    image: cvat.custom.ninerouter.vision.boxmask
    baseImage: python:3.11-slim
    directives:
      preCopy:
        - kind: RUN
          value: pip install --no-cache-dir requests pillow pyyaml

  platform:
    attributes:
      restartPolicy:
        name: always
        maximumRetryCount: 3
      mountMode: volume
